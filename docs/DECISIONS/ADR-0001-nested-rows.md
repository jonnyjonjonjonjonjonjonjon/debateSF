# ADR-0001: Nested Per-Parent Rows
Decision: child rows live inside parent column; no global depth rows; no absolute layout.
Rationale: widths scoped to parent; creation order irrelevant; fewer bugs.
